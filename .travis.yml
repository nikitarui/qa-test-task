sudo: required

services:
  - docker

before_install:
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
  - docker build -t \"$DOCKER_USERNAME\"/test
  - docker push \"$DOCKER_USERNAME\"/test